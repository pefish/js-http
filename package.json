{
  "name": "@pefish/js-http",
  "version": "0.2.1",
  "description": "http tool of js",
  "keywords": [
    "common",
    "js",
    "tool"
  ],
  "homepage": "https://github.com/pefish/js-http",
  "bugs": "https://github.com/pefish/js-http/issues",
  "license": "MIT",
  "author": "pefish",
  "types": "./types/http.d.ts",
  "engines": {
    "node": "*"
  },
  "main": "lib/http.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/pefish/js-httprequest"
  },
  "scripts": {
    "build": "rm -rf lib types && tsc",
    "test": "mocha --timeout 10000 --require ts-node/register src/**/*.test.ts",
    "t": "mocha --timeout 10000 --require ts-node/register",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.7",
    "ts-node": "^8.1.0",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "@pefish/js-util-desensitize": "^0.1.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.8"
  }
}
